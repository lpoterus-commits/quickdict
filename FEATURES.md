# 快捷查词助手 · QuickDict 2.0

**给韩语学习者的 macOS 取词工具。** 屏幕上任何地方的韩语——扫描版教材、PDF、视频字幕、网页、别人发来的截图——
框选或划词，立刻弹出词典释义。识别全部在本机完成，不联网。

---

## 功能介绍

### 两种取词方式，覆盖屏幕上所有文字

| | 适用场景 |
|---|---|
| **截图取词** `⌃⌥9` | 图片、PDF 扫描件、视频字幕、别人发的截图——**选不中的字** |
| **划词取词** `⌘⌥D` | 网页、文档里能选中的文本，没有识别误差，也不用框选 |

这两条路是互补的：韩语学习资料大量是影印本和讲义 PDF，鼠标根本选不中；而能选中的地方用 OCR 反而多此一举。

### 三种动作，一次截图分流

| | |
|---|---|
| **查词** `⌃⌥9` | 判断语种 → 打开对应词典 |
| **转文字** `⌃⌥0` | OCR → 智能拼行 → 只放剪贴板，不查词 |
| **扫码** `⌃⌥8` | 识别二维码/条形码 → 是网址就用浏览器打开 |

划词侧还有四个直达键：`⌘⌥N` Naver、`⌘⌥P` Papago、`⌘⌥G` 谷歌翻译，跳过语种判断直接指定词典。

### 结果悬浮窗

- `⌘1`…`⌘9` 一键换词典，同一个词在不同词典之间来回对照
- `⌘L` 改词条重查——OCR 认错字时不用重新框选
- `esc` 关闭；图钉按钮可常驻置顶，一边查一边抄写
- 罗盘按钮把当前查询丢到默认浏览器

### 词典完全可配

默认六个：Naver 韩中、국어사전（韩韩）、Papago、谷歌翻译、Google、Google AI 模式。

配置是一个 JSON 数组，`{q}` 是查询词的位置。加一个新词典就是加一行：

```json
{ "id": "krdict", "name": "KRDict", "languages": ["ko"],
  "url": "https://krdict.korean.go.kr/eng/dicSearch/search?query={q}" }
```

`languages` 决定什么时候自动命中，`"*"` 是兜底，空数组表示只能手动切。

### 快捷键可视化设置

菜单栏 →「快捷键设置」：点按键框，直接按下想要的组合。每行还能改取字方式、动作、目标词典。
保存时当场重新注册，**被别的程序占用的组合会立刻标红**，不用等你按下去才发现没反应。

### 内置说明书

`⌘?` 随时调出，**内容按当前配置实时生成**——改了快捷键或加了词典，说明会跟着变，不会出现文档和实际对不上。

---

## 特色功能

这几条是市面上其他取词工具没有的。

### ★ 韩语断词还原

**这是这个软件存在的理由。**

OCR 一段换行的韩语时，行尾的词会被切成两半。`이성적인`（理性的）跨行时变成 `이` + `성적인`，
直接拼接就成了 `이 성적인`——意思完全变了。所有 OCR 工具都有这个问题，因为**换行处的空格被吃掉了，像素上不存在**。

解决它撞了三堵墙：

| 尝试 | 结果 |
|---|---|
| 系统韩语拼写检查 | ✗ `그리느끼는` 这种随便拼的都判成词，只查谚文音节合法性 |
| NaturalLanguage 韩语分词器 | ✗ 只按空格切，不做形态素分析 |
| 纯几何（行末是否顶满） | △ 能分辨换行和段落结束，但分不清断点落在空格还是词中间 |

最终用**三信号组合**：

1. **系统韩语词典**（`DCSCopyTextDefinition`）——把两半拼起来，看最长成词前缀有没有**越过接缝**。
   `이` + `성적인` → 查到 `이성적`（长度 3 > 上半的 1）→ 判定断词
2. **行末几何**——Vision 给的每行 bounding box，判断这行是被挤断的还是自然结束
3. **韩语词尾表**——行首出现悬空的助词或变位词尾（`은`/`는`/`로서`/`하며`/`진다고`…）

拉丁文另走一路：查英语拼写词典，`jum` + `ps` = `jumps` 成词 → 拼回去。

**实测 47 个用例通过 45 个**（6 段韩语 + 英语 + 意大利语，多种栏宽含强制逐字断行）。

### ★ 判不了的地方明确标出，而不是假装解决

`이성적인`（理性的）和 `이 성적인`（这个性方面的）**都是合法韩语**。断行落在中间时，
几何、词典、语法三条路全都分辨不了——只有上下文能判，那需要语言模型。

所以做法是：默认合并（合起来的用法频率高得多），但**把这类位置报出来**：

```
已复制 87 字
⚠️ 这些地方合并了，但拆开写也讲得通：이성적인
```

判定条件是「由词典判据促成合并，且上半截本身也成词」。实测每段文本报 0~1 处，不会变成噪音。

### ★ 识别全程本地，断网可用

Vision OCR、Vision 条码、NaturalLanguage 判语种、系统词典、几何计算——**全部端上推理**。
实测运行期间零网络连接，源码里 `URLSession` 出现 0 次。

只有「打开词典网页」需要联网，那本来就是网页。`⌃⌥0` 截图转文字**完全离线可用**。

App 自身不记录任何查询历史；内嵌窗口的 Cookie 和 localStorage 默认退出即清（词典网站会往那里塞「最近搜索」）。

### 语种判不准时会告诉你

单个拉丁词的语种识别本质不可靠——`radio`、`via`、`piano` 在英语和意语里都成立。
判定置信度低于阈值时，窗口左上角的语种标签会变**橙色带问号**，一眼看出该手动切。

设计上不追求猜对，而是**猜错时让你立刻发现、一键改过来**。

### 划词不破坏剪贴板

优先用 Accessibility API 直读选区，完全不碰剪贴板。
只有目标 App 不暴露选区时才回落到模拟 `⌘C`——而且轮询而非固定等待，读完**立刻**原样还原，
整条剪贴板（含图片、富文本）都存下来再恢复，不会把你剪贴板里的图片弄丢。

### 窗口层级跟随焦点

不是简单的「置顶 / 不置顶」。弹出瞬间浮到最上层确保你看得见，**你一点别的窗口它就自动沉下去**，不挡路。
按图钉才常驻置顶。

（这一条是必须的：本 App 是菜单栏常驻类型，macOS 26 下抢不到前台，只用普通层级的话窗口会开在别的窗口后面。）

### 界面跟随系统语言

简体中文和英文，按系统语言自动切换。加一种语言只需在 `Resources/` 下新建一个
`<语言代码>.lproj/Localizable.strings`，**不用改任何代码**。

---

## 环境要求

- macOS 13.0 或更高
- Apple Silicon 或 Intel（通用二进制）
- 两个权限：**屏幕录制**（截图取词）、**辅助功能**（划词取词）

## 切换查词语言

默认查到中文。换成别的母语，改配置里的子域名和目标语言代码即可：

| 母语 | Naver 词典 | Papago | 谷歌翻译 |
|---|---|---|---|
| 中文 | `zh.dict.naver.com` | `tk=zh-CN` | `tl=zh-CN` |
| 英语 | `en.dict.naver.com` | `tk=en` | `tl=en` |
| 日语 | `ja.dict.naver.com` | `tk=ja` | `tl=ja` |

Naver 各语言对的路径完全一样，只换子域名就行。改完在菜单栏点「重新载入配置」立即生效。
